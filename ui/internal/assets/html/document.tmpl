{{define "document"}}
<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{template "page:title" .}} - AIS Test Bench</title>
    {{- range .Resources.Stylesheets}}
    <link rel="stylesheet" href="{{.URL}}"{{with .Integrity}} integrity="{{.}}" crossorigin=""{{end}}>
    {{- end}}
    {{- range .Resources.Scripts}}
    <script defer src="{{.URL}}"{{with .Integrity}} integrity="{{.}}" crossorigin=""{{end}}></script>
    {{- end}}
</head>
<body>
    <header>
        {{if .HomeURL}}<a href="{{.HomeURL}}">AIS Test Bench</a>{{else}}<span>AIS Test Bench</span>{{end}}
        <nav>
            {{- with .ManagerURL}}
            <a href="{{.}}">Manager</a>
            {{- end}}
            {{- with .DisplayURL}}
            <a href="{{.}}">Display</a>
            {{- end}}
        </nav>
    </header>
    <main>
        {{template "page:content" .}}
    </main>
</body>
</html>
{{end}}

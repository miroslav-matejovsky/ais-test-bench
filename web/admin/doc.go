// Package admin embeds the independent admin frontend asset bundle.
// Files contains only this frontend's assets directory; bootstrap uses fs.Sub
// before mounting it. Dependencies: embed only. REST calls use /api/v1.
package admin

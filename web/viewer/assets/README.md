# Viewer assets

`index.html` is the current scaffold page. This directory is the distribution root
embedded by the parent Go package. Future frontend builds place their HTML, CSS,
JavaScript, and local media here. Asset URLs must respect `/viewer/`.

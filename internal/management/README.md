# Management

Design contracts for validated scenario and target management plus system status.
The running manager UI calls the concrete simulation package through HTTP handlers.

Subpackages document their public contracts, responsibilities, and dependencies in
`doc.go`. See the [running architecture](../../README.md).

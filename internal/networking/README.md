# Networking

TCP client streams and UDP destinations. Transport adapters consume complete NMEA lines and own sockets, queues, deadlines, and connection state.

Subpackages document their public contracts, responsibilities, and dependencies in
`doc.go`. See [the package catalog](../../docs/packages.md).

// Package middleware declares HTTP wrapping functions for request IDs, panic
// recovery, structured access logs, request limits, and metrics. It depends on
// net/http and observability libraries, never on domain or persistence packages.
package middleware

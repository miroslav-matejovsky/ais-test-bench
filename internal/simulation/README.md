# Simulation

Scenario definitions, lifecycle, virtual time, deterministic playback, and orchestration of target movement and AIS traffic. The application layer owns the active run.

Subpackages document their public contracts, responsibilities, and dependencies in
`doc.go`. See [the package catalog](../../docs/packages.md).
